import 'package:flutter/material.dart';

class CoinIcon extends Icon {
  const CoinIcon({Key? key, double? size})
      : super(
          Icons.monetization_on,
          key: key,
          color: Colors.yellow,
          size: size,
        );
}
